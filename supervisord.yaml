init_config:

instances:
    - name: server0
      host: localhost
      port: 9001
      user: user # optional
      pass: pass # optional
      proc_names:
       - apache2
       - custom_app
       - etc
