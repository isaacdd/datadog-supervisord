init_config:

instances:
    - name: server0
      host: localhost
      port: 9001
      user: user # optional
      pass: pass # optional
      proc_names: # optional. will monitor all processes if not specified
       - apache2
       - custom_app
       - etc
    - name: server1
      etc
